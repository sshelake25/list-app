import { Demo1Directive } from './demo1.directive';

describe('Demo1Directive', () => {
  it('should create an instance', () => {
    const directive = new Demo1Directive();
    expect(directive).toBeTruthy();
  });
});
