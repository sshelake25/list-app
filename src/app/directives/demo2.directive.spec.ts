import { Demo2Directive } from './demo2.directive';

describe('Demo2Directive', () => {
  it('should create an instance', () => {
    const directive = new Demo2Directive();
    expect(directive).toBeTruthy();
  });
});
